# Entry-flow review

Run `node --test tests/filmmaand-entry/access.test.mjs` for the six routing/recovery guards.
Run `npm ci --omit=dev --no-audit --no-fund` in `filmmaand-server`, then from the repository root run `node tests/filmmaand-entry/server.mjs`.

The review server binds to 127.0.0.1:4312, uses an isolated in-memory store, and captures mail locally. It never calls a mail provider. Only `entry@example.test` receives a synthetic code; GET `/__fixture` returns it and request records. POST `/__fixture/expire` expires this fixture's sessions and resets its test rate limits. No participant data survives restart. The fixture freezes the initial ballot to blade/matrix/lotr, matching the existing-ballot condition needed to demonstrate next-round points without changing ballot semantics.

`ENTRY_PORT=4313 ENTRY_LOCK_ONLY=1 ENTRY_DELAY_ART=1` is the historical lock-review mode: it serves Git-pinned lock assets (47b453e) and baseline UI (29f10dff), deliberately delaying artwork. It requires those review commits in the local repository.

## Browser evidence

Actual isolated browser interactions, 7 September 2026:

- Public `/filmmaand/` landed on Programma. Header login, real local code verification, profile and avatar save led to `/filmmaand/films/`. The first tutorial step was Leaderboard. No availability modal and no voting navigation.
- Chose Horror night and Indiana Jones through their actual heart buttons. The tutorial did not advance while the server had not saved both choices. No preferences were generated by the tutorial.
- Actual mouse down/move/move/up moved Horror behind Indiana. Saved `rankingOrder` became `["indiana","horror"]`; only then did the tutorial show the result step. `entry-golden-drag-frame.png` captures the real in-flight drag.
- A 390×844 keyboard exercise moved Indiana right. Saved order became `["horror","indiana","anime"]`; the actual approved r19 leaderboard showed 3, 2, and 1 points respectively. No horizontal overflow. `entry-golden-mobile-result.png` shows the saved consequence.
- Social followed the ranking exercise. Calendar was an explicitly optional next step, with a working finish action. No automatic standalone availability introduction.
- One hostile recovery: expired the fixture session, then selected anime. Receipt key `8202a952-2894-4b1f-ae07-9c66539dcf42`, expectedRevision 2, choices `["horror","indiana","anime"]`, rankingOrder `["indiana","horror"]`. The canonical request received 401. Completed the real login UI. The second PUT had exactly the same idempotency key and byte-identical body. Saved revision became 3, pending receipt cleared, `performance.timeOrigin` remained unchanged, route stayed Films, and no intro appeared. Profile and existing ranking were preserved.

The initial fixture had an unfrozen, derived ballot; its first liked films entered that ballot and were therefore excluded from the next-round result. This was not changed in product code. For the visible-points proof, the fixture's organizer endpoint froze blade/matrix/lotr before the keyboard exercise. The fixture now starts in that same explicit state. The tour shows personal points honestly when all chosen films are already in the current ballot.

## Integration boundaries

- Captain owns real root/program/agenda redirects and static HTML navigation ordering. Runtime shell uses Programma first, then Films, and points the logo to `/filmmaand/program/`.
- The approved r19 `films/next-round.js/css` are copied byte-for-byte; ranking and voting semantics are untouched.
- Access render helpers, early artwork promise and CSS retain the reviewed lock fixes. New fresh-login routing and recovery guards are the separate full-entry change.
- Browser proof covers real mouse drag and mobile keyboard input; no new automated touch-gesture proof is claimed.

### Captain route/static-HTML integration check

Applied captain f2178f1 and 8cfaec0 as 948a8bd and 1c0612f in this isolated worktree. The fixture now invokes the real entry handler for root redirects and mirrors the three exact Netlify legacy redirects. Seven focused tests passed. Browser checks confirmed Programma first in header and footer, logo `/filmmaand/program/`, correct active Programma item, legacy agenda/index query preservation, and protected Films retaining the login lock. HTTP checks confirmed root 302→Programma, edit/thanks 302→Stemmen, and all three legacy Agenda variants 301→Programma. No additional personal writes were needed for this static/route-only delta.

import {readFile} from 'node:fs/promises';import {randomUUID} from 'node:crypto';
const [id,startsAt,...choices]=process.argv.slice(2);if(!id||!startsAt||!choices.length)throw Error('Usage: node planning/admin.mjs PLAN_ID UTC_START OPTION_ID [OPTION_ID...]');
const token=(await readFile(new URL('./.data/admin-token',import.meta.url),'utf8')).trim();const response=await fetch((process.env.PLANNING_API_BASE||'http://127.0.0.1:8789/api')+'/plans/'+encodeURIComponent(id)+'/confirmation',{method:'POST',headers:{Authorization:'Bearer '+token,'Content-Type':'application/json','Idempotency-Key':process.env.PLANNING_REQUEST_KEY||randomUUID()},body:JSON.stringify({startsAt,choices})});const result=await response.json();console.log(JSON.stringify(result,null,2));if(!response.ok)process.exitCode=1;
